---
interface Props {
  as?: "p" | "span" | "div";
  class?: string;
}

const { as: Tag = "p", class: className = "" } = Astro.props;
---

<Tag
  class:list={[
    "text-xs font-medium uppercase tracking-[0.16em] text-neutral-500",
    className,
  ]}
>
  <slot />
</Tag>
